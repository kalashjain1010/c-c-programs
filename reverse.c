#include<stdio.h>

int main(){
    int num;
    printf("enter the no.\n");
    scanf("%d",&num);
    while (num>0)
    {
        printf("your no. is %d\n", num);
        num--;

    }
    
    return 0;
